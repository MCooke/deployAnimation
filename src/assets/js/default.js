function stage1(){
	var $dev = $('.item-dev');
	var $code = $('.item-code');
	var $github = $('.item-github');

	$dev.addClass('infinite').addClass('pulse');
	$code.addClass('slideInLeft').removeClass('hide');
}